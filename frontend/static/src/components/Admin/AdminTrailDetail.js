import { useParams } from "react-router-dom";

function AdminTrailDetail() {
    let params = useParams();
    
    get

    return (
        <div> Trail {params.trailId}</div>
    )
}

export default AdminTrailDetail